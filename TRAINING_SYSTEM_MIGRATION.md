# Quick Guide: Migrating train_models_compare.bat System

## 🎯 What Gets Migrated

This migration focuses **ONLY** on `train_models_compare.bat` and everything it needs:

### ✅ Included Files (6 core files)
1. **dataset-gen.c** - Generates minimax-evaluated datasets
2. **dataset_processor.c** - Splits datasets into train/test sets
3. **naive_bayes.c** - Naive Bayes training algorithm
4. **linear_regression.c** - Linear Regression training algorithm  
5. **q_learning.c** - Q-Learning (reinforcement learning)
6. **train_models_compare.bat** - The main automation script

### 📚 Documentation
- Q_LEARNING_GUIDE.md
- TRAINING_GUIDE.md  
- README.md (auto-generated)
- .gitignore (auto-generated)

### Optional (use flags)
- Dataset files (*.data) - use `-IncludeDatasets`
- Trained models - use `-IncludeModels`

---

## 🚀 How to Use

### Basic Migration (minimal)
```powershell
cd c:\Users\muhdh\Documents\GitHub\tic-tac-toe-ml-training
.\migrate-training-system.ps1
```

This creates: `c:\Users\muhdh\Documents\GitHub\ttt-ml-training\`

### With Datasets
```powershell
.\migrate-training-system.ps1 -IncludeDatasets
```

### With Datasets AND Trained Models
```powershell
.\migrate-training-system.ps1 -IncludeDatasets -IncludeModels
```

### Custom Location
```powershell
.\migrate-training-system.ps1 -NewRepoPath "c:\custom\path\my-ml-project"
```

### All Options Combined
```powershell
.\migrate-training-system.ps1 -NewRepoPath "c:\ml\ttt-trainer" -IncludeDatasets -IncludeModels
```

---

## 📁 New Repository Structure

```
ttt-ml-training/           # New repository root
├── dataset-gen.c
├── dataset_processor.c
├── naive_bayes.c
├── linear_regression.c
├── q_learning.c
├── train_models_compare.bat    # Main script (paths updated!)
│
├── models/                     # Model output directories
│   ├── naive_bayes_non_terminal/
│   ├── naive_bayes_combined/
│   ├── linear_regression_non_terminal/
│   └── linear_regression_combined/
│
├── Q_LEARNING_GUIDE.md
├── TRAINING_GUIDE.md
├── README.md
└── .gitignore
```

**Key Changes:**
- ✅ Flat structure (all C files in root)
- ✅ Batch file paths updated automatically
- ✅ Models save to `models\` subdirectories
- ✅ All self-contained and ready to run

---

## ⚡ After Migration - Quick Start

```powershell
# Navigate to new repo
cd c:\Users\muhdh\Documents\GitHub\ttt-ml-training

# Generate datasets (if not included)
gcc dataset-gen.c -o dataset-gen.exe
.\dataset-gen.exe    # Choose option 3 (both datasets)

# Run complete training
.\train_models_compare.bat

# Done! Check results
tree output /F
```

---

## 🎯 What The Script Does

1. ✅ Creates new directory structure
2. ✅ Copies 5 C source files
3. ✅ Copies and **updates** batch file paths
4. ✅ Copies documentation
5. ✅ Creates README.md and .gitignore
6. ✅ Optionally copies datasets/models
7. ✅ Shows summary and next steps

### Path Updates (Automatic)
The script automatically fixes these paths in the batch file:

| Old Path | New Path |
|----------|----------|
| `..\\src\\naive_bayes.c` | `naive_bayes.c` |
| `..\\src\\linear_regression.c` | `linear_regression.c` |
| `..\\models\\` | `models\\` |
| `..\\dataset\\processed` | `models\\temp` |

---

## 📋 Checklist After Migration

```powershell
# 1. Test compilation
cd ttt-ml-training
gcc dataset-gen.c -o dataset-gen.exe
gcc dataset_processor.c -o dataset_processor.exe
gcc naive_bayes.c -o naive_bayes.exe -lm
gcc linear_regression.c -o linear_regression.exe -lm
gcc q_learning.c -o q_learning.exe -lm

# 2. Generate datasets (if needed)
.\dataset-gen.exe

# 3. Run training
.\train_models_compare.bat

# 4. Verify outputs
dir models
dir output

# 5. Initialize git
git init
git add .
git commit -m "Initial commit: ML training system"

# 6. Push to GitHub
git remote add origin https://github.com/username/repo.git
git push -u origin main
```

---

## 🔍 What's NOT Included

### Files You DON'T Need
- ❌ Old CSV-based training system (`src/` original files)
- ❌ Evaluation programs (not part of training)
- ❌ AI vs AI test programs  
- ❌ Build scripts for other systems
- ❌ .git folder and history

### Can Be Regenerated
- Datasets (*.data) - generated by dataset-gen.exe
- Trained models - generated by train_models_compare.bat
- Output folder - created automatically during training
- Executable files (*.exe) - compile in new location

---

## ⚙️ Customization Options

### Include Datasets
```powershell
.\migrate-training-system.ps1 -IncludeDatasets
```
**Pros:** Ready to train immediately  
**Cons:** Larger repo size (~100KB)

### Include Trained Models
```powershell
.\migrate-training-system.ps1 -IncludeModels
```
**Pros:** Reference models included  
**Cons:** Larger repo, may want fresh training

### Neither (Minimal)
```powershell
.\migrate-training-system.ps1
```
**Pros:** Smallest size, clean start  
**Cons:** Need to generate datasets first

---

## 🎮 Complete Example Run

```powershell
# Step 1: Run migration
PS> cd c:\Users\muhdh\Documents\GitHub\tic-tac-toe-ml-training
PS> .\migrate-training-system.ps1 -IncludeDatasets
Continue? (y/n): y

[1/8] Creating directory structure...
  ✓ Directories created
[2/8] Copying source files...
  ✓ dataset-gen.c
  ✓ dataset_processor.c
  ✓ q_learning.c
  ✓ naive_bayes.c
  ✓ linear_regression.c
[3/8] Copying batch file...
  ✓ train_models_compare.bat (paths updated)
[4/8] Copying documentation...
  ✓ Q_LEARNING_GUIDE.md
  ✓ TRAINING_GUIDE.md
[5/8] Copying datasets...
  ✓ tic-tac-toe-minimax-non-terminal.data
  ✓ tic-tac-toe-minimax-complete.data
[6/8] Skipping trained models
[7/8] Creating README.md...
  ✓ README.md
  ✓ .gitignore
[8/8] Complete!

✅ MIGRATION COMPLETE
Location: c:\Users\muhdh\Documents\GitHub\ttt-ml-training

# Step 2: Navigate and run
PS> cd ..\ttt-ml-training
PS> .\train_models_compare.bat

# Training runs automatically...

# Step 3: Check results
PS> tree output /F
```

---

## 💡 Tips

### Tip 1: Don't Include Datasets
Regenerate them in the new location for a clean start:
```powershell
.\dataset-gen.exe  # Takes ~5 seconds
```

### Tip 2: Fresh Training is Better
Don't include old models - train fresh in new location:
```powershell
.\train_models_compare.bat  # Takes ~5 minutes
```

### Tip 3: Use Git from the Start
```powershell
cd ttt-ml-training
git init
git add .
git commit -m "Initial setup"
# Then generate datasets and train
```

### Tip 4: Test Before Committing
```powershell
# Make sure everything works
.\train_models_compare.bat
# Then commit the results
git add models/ output/
git commit -m "Initial training results"
```

---

## 🆚 Comparison: Full vs Training-Only Migration

| Feature | Full Migration | Training-Only |
|---------|---------------|---------------|
| **Size** | ~50+ files | 6 core files |
| **Complexity** | Both systems | One focused system |
| **Structure** | Multi-folder | Flat/simple |
| **Focus** | Everything | Training pipeline only |
| **Recommended** | Archive/history | New project ✅ |

---

## ❓ FAQ

**Q: Will the batch file work after migration?**  
A: Yes! The script automatically updates all paths.

**Q: Do I need to modify anything manually?**  
A: No! Everything is configured automatically.

**Q: Should I include datasets?**  
A: Optional. They regenerate in ~5 seconds anyway.

**Q: What if I want to include other files?**  
A: Manually copy them after migration, or use the full migration script.

**Q: Can I rename files/folders?**  
A: Yes, but update the batch file paths accordingly.

---

## 🎯 Summary

**One command to migrate everything:**
```powershell
.\migrate-training-system.ps1 -IncludeDatasets
cd ..\ttt-ml-training
.\train_models_compare.bat
```

**That's it!** Your complete training system is ready to go. 🚀
